---
layout: post
title: "Pluto SDR CW 레이더 실험 코드"
subtitle: "주파수 제한 해제 및 실험 코드 정리"
date: 2025-11-24
categories: Code         # <- 카테고리를 Code로 설정!
tags: [pluto-sdr, python]
---

### 📡 SDR 주파수 제한 범위 늘리기
Pluto SDR의 주파수 제한 범위를 약 6 GHz까지 늘리는 코드입니다.

```python
# 여기에 파이썬 코드를 넣으세요
import adi

print("PlutoSDR 연결 시도 중...")

try:
    # 1. 연결
    sdr = adi.Pluto("ip:192.168.2.1")
    
    # 2. 5.8 GHz 설정 시도 (원래는 안 되는 주파수)
    sdr.rx_lo = 5800000000 # 5.8 GHz
    sdr.tx_lo = 5800000000
    
    # 3. 설정된 값 읽어오기
    real_rx = int(sdr.rx_lo)
    
    print("-" * 30)
    if real_rx == 5800000000:
        print("🎉 [성공!] 5.8 GHz 설정이 완료되었습니다.")
        print("이제 설계하신 안테나를 꽂고 실험을 시작하세요!")
    else:
        print(f"[실패] 설정된 주파수: {real_rx} Hz")
        print("해킹 과정이 제대로 적용되지 않았습니다.")
    print("-" * 30)

except Exception as e:
    print("❌ 에러 발생:", e)
    print("USB 연결을 확인하거나, 드라이버가 잡혔는지 확인해주세요.")
```